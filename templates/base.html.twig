<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    
    <title>{% block title %}Mon Site E-commerce{% endblock %}</title>

    {# CSS global #}
    <link rel="stylesheet" href="{{ asset('build/app.css') }}">
    
    {# Bloc pour CSS spécifiques à certaines pages #}
    {% block stylesheets %}{% endblock %}
</head>
<body>
    {# ===== HEADER / NAVBAR ===== #}
    <header>
        <nav class="navbar">
            <ul>
                <li><a href="{{ path('home') }}">Accueil</a></li>
                <li><a href="{{ path('product_list') }}">Produits</a></li>
                <li><a href="{{ path('cart') }}">Panier</a></li>
                <li><a href="{{ path('account') }}">Compte</a></li>
            </ul>
        </nav>
    </header>

    {# ===== CONTENU PRINCIPAL ===== #}
    <main>
        {% block body %}{% endblock %}
    </main>

    {# ===== FOOTER ===== #}
    <footer>
        <p>© 2025 Mon Site E-commerce. Tous droits réservés.</p>
    </footer>

    {# JS global #}
    <script src="{{ asset('build/app.js') }}"></script>

    {# Bloc pour JS spécifiques à certaines pages #}
    {% block javascripts %}{% endblock %}
</body>
</html>
